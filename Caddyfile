# LAN HTTPS uses Caddy's internal CA. Browsers will trust it only after the
# Caddy root CA is installed on each client device.
192.168.1.201 {
	tls internal
	reverse_proxy localhost:3000
	encode zstd gzip
}
