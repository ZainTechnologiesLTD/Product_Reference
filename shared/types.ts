/**
 * Unified type exports
 * Import shared types from this single entry point.
 */

export type {
  User,
  InsertUser,
  Session,
  Category,
  InsertCategory,
  Product,
  InsertProduct,
  AuditLog,
} from "../drizzle/schema";
